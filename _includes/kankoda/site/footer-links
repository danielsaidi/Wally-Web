{%- for path in include.paths -%}
  {%- assign current-page = site.pages | where: "path", path | first -%}
  {%- if current-page.title -%}
  <a href="{{ current-page.url | relative_url }}">{{ current-page.title | escape }}</a>
  {%- endif -%}
{%- endfor -%}
